<div class="figma"></div>

<style lang="scss">
  .figma {
    aspect-ratio: 1 / 1;
    position: relative;
    width: var(--step-5);
    border: 1px solid var(--foreground);
    border-radius: 50%;
    border-bottom-left-radius: var(--radius);
    &::after {
      content: '';
      position: absolute;
      border-radius: 50%;
      background-color: var(--background);
      width: var(--step--1);
      height: var(--step--1);
      inset: 0 0 auto auto;
      border: inherit;
    }
  }
</style>
