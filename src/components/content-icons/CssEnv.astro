<div class="css-env"></div>

<style lang="scss">
  .css-env {
    aspect-ratio: 1 / 1;
    position: relative;
    width: var(--step-5);
    border: 1px solid var(--foreground);
    border-block-start: 0;
    border-radius: 0 0 25% 25%;
    &::after {
      content: '';
      position: absolute;
      inset: 75% 25%;
      width: calc(50% + 1px);
      height: 1px;
      background-color: var(--foreground);
    }
  }
</style>
