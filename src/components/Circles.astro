<div class="circles" aria-hidden="true"></div>

<style lang="scss">
  @use '../styles/mixins' as *;
  .circles {
    position: absolute;
    inset: 0;
    margin: auto;
    pointer-events: none;
    width: 40vw;
    max-width: 35rem;
    aspect-ratio: 1 / 1;
    mix-blend-mode: difference;
    z-index: 1;
    @include medium {
      transform: rotate(90deg);
      width: 75vw;
    }
    &::after,
    &::before {
      content: '';
      position: absolute;
      width: 100%;
      height: 100%;
      border-radius: 50%;
      border-top-right-radius: 0;
      background-image: linear-gradient(45deg, var(--accent), var(--background));
      mix-blend-mode: lighten;
      html[data-scheme='light'] & {
        mix-blend-mode: darken;
      }
    }
    &::after {
      margin-inline-start: -25%;
    }
    &::before {
      margin-inline-start: 25%;
      transform: rotate(180deg);
    }
  }
</style>
