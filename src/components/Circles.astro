<div class="circle">
  <div class="circle__inner"></div>
  <div class="circle__inner"></div>
</div>

<style lang="scss">
  .circle {
    display: flex;
    justify-content: center;
    margin-bottom: var(--step-5);
    &__inner {
      aspect-ratio: 1 / 1;
      background-image: linear-gradient(45deg, var(--foreground), var(--background));
      border-radius: 50%;
      border-top-right-radius: 0;
      max-width: 500px;
      mix-blend-mode: lighten;
      transform: translate(25%);
      width: 50vw;
      &:nth-child(2) {
        background-image: linear-gradient(45deg, var(--background), var(--foreground));
        border-radius: 50%;
        border-bottom-left-radius: 0;
        transform: translate(-25%);
      }
    }

    @at-root {
      html[data-scheme='light'] .circle__inner {
        mix-blend-mode: darken;
      }
    }
  }
</style>
