---
const { icon } = Astro.props;
import {
  AiIcon,
  EthicsIcon,
  HopperIcon,
  ImposterIcon,
  InternetIcon,
  NewYorkIcon,
  ObserverIcon,
  ScanningIcon,
  ShareIcon,
  WebsiteIcon,
} from './content-icons';
---

<!-- TODO: Make all of this much simpler -->{icon === 'ai' && <AiIcon />}
{icon === 'ethics' && <EthicsIcon />}
{icon === 'hopper' && <HopperIcon />}
{icon === 'imposter' && <ImposterIcon />}
{icon === 'internet' && <InternetIcon />}
{icon === 'new-york' && <NewYorkIcon />}
{icon === 'observer' && <ObserverIcon />}
{icon === 'scanning' && <ScanningIcon />}
{icon === 'share' && <ShareIcon />}
{icon === 'website' && <WebsiteIcon />}
