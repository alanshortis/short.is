<figure class="sketch">
  <div class="cube" aria-hidden="true">
    <div></div>
    <div></div>
    <div></div>
    <div></div>
    <div></div>
    <div></div>
  </div>
  <figcaption class="vh">
    A small abstract animation made in code. A cube, which appears to be illuminated from the inside as each
    edge and corner is a little darker. The cube is spinning in all three dimensions.
  </figcaption>
</figure>

<style lang="scss">
  @use '../../styles/mixins' as *;

  $offset: 100px;

  .sketch {
    color-scheme: dark;
    background-color: var(--background);
    perspective: 600px;
    @include sketch-container();
  }

  .cube {
    @keyframes spin {
      100% {
        transform: rotate3d(1, 2, 0, 1turn);
      }
    }
    animation: spin 5s linear infinite;
    aspect-ratio: 1 / 1;
    position: relative;
    transform-style: preserve-3d;
    width: $offset * 2;
    div {
      background-color: hsl(0, 0%, 100%);
      box-shadow: inset 0 0 50px 0 hsl(0, 0%, 0%);
      height: 100%;
      position: absolute;
      width: 100%;
      &:nth-child(1) {
        transform: translateZ($offset);
      }
      &:nth-child(2) {
        transform: translateZ(-$offset) rotateY(180deg);
      }
      &:nth-child(3) {
        transform: translateX(-$offset) rotateY(-90deg);
      }
      &:nth-child(4) {
        transform: translateX($offset) rotateY(90deg);
      }
      &:nth-child(5) {
        transform: translateY(-$offset) rotateX(90deg);
      }
      &:nth-child(6) {
        transform: translateY($offset) rotateX(-90deg);
      }
    }
  }
</style>
