<div class="circle">
  <span class="circle__alt">
    An image made using code. Minimal graphic design style. Two circles, both with a gradient from white to
    black, intersect and appear to merge together into one shape.</span
  >
</div>

<style lang="scss">
  @use '../../styles/mixins' as *;

  .circle {
    display: flex;
    height: 100%;
    width: 100%;
    justify-content: center;
    align-items: center;
    &::before,
    &::after {
      aspect-ratio: 1 / 1;
      background-image: linear-gradient(45deg, var(--foreground), var(--background));
      border-radius: 50%;
      border-top-right-radius: 0;
      content: '';
      inline-size: clamp(0%, 100%, 28.33rem);
      mix-blend-mode: lighten;
      transform: translate(25%);
      html[data-scheme='light'] & {
        mix-blend-mode: darken;
      }
    }
    &::after {
      transform: translate(-25%) rotate(180deg);
    }
    &__alt {
      @include visually-hidden;
    }
  }
</style>
