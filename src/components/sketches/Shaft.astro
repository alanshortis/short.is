<div class="shaft">
  <span class="shaft__alt">
    An image made using code. Minimal graphic design style. in a dark space, a bright and narrow slit allows
    in light which casts a long shaft of light below, eventually fading to black. The source of light appears
    to glow slightly.
  </span>
</div>

<style lang="scss">
  @use '../../styles/mixins' as *;

  .shaft {
    background-image: linear-gradient(0deg, var(--dark), hsl(0, 0%, 25%));
    flex: 1;
    height: 75vh;
    max-width: 10rem;
    position: relative;
    transform: skew(22.5deg) rotate(22.5deg);
    width: 100%;
    &::before {
      position: absolute;
      content: '';
      width: 100%;
      height: 0.25rem;
      background-color: var(--light);
      border-radius: 0.25rem;
      box-shadow: 0 0 3rem 0.25rem var(--light);
    }
    &__alt {
      @include visually-hidden;
    }
  }
</style>
