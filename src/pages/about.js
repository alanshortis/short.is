import React from 'react';
import { Layout, Meta } from '../components';

const About = () => (
  <>
    <Meta title="About" />
    <Layout isDark>Alan Shortis is a web developer.</Layout>
  </>
);

export default About;
