import React from 'react';
import { Layout, Meta } from '../components';

const Writing = () => (
  <>
    <Meta title="Writing" />
    <Layout isDark>Writing</Layout>
  </>
);

export default Writing;
