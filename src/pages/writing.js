import React from 'react';
import { Layout, Meta, Header } from '../components';

const Writing = () => (
  <>
    <Meta title="Writing" />
    <Layout isDark>
      <Header />
      Writing
    </Layout>
  </>
);

export default Writing;
