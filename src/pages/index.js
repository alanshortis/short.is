import React from 'react';
import { Layout, Meta, Header } from '../components';

const Home = () => (
  <>
    <Meta title="Home" />
    <Layout isDark>
      <Header />
      Home
    </Layout>
  </>
);

export default Home;
