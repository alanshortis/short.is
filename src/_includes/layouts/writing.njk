{% from "macros/postdate.njk" import postDate %}
{% extends 'layouts/main.njk' %}

{% block content %}
  <article class="post-content">
    {{ postDate(date, updated) }}
    <h1>{{ title }}</h1>
    <p class="large">{{ intro }}</p>
    {{ content | safe }}
  </article>
  {%- include "nextprev.njk" %}
{% endblock %}
