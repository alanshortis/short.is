<!DOCTYPE html>
<html lang="en-GB" id="top">
  <head>
    {%- set pageTitle = title + ' — ' + site.title if title else site.title %}
    <meta charset="UTF-8">
    <title>{{ pageTitle }}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no"/>
    <meta name="description" content="{{ intro | default(site.description) }}"/>
    <meta property="og:title" content="{{ pageTitle }}"/>
    <meta property="og:description" content="{{ intro | default(site.description) }}"/>
    <meta property="og:url" content="{{ site.url }}{{ page.url }}"/>
    {# <meta property="og:image" content="/og.png"/> #}
    <meta name="twitter:card" content="summary"/>
    <meta name="twitter:creator" content="{{ site.twitter }}"/>
    {# <link rel="icon" href="/icon.png"/> #}
    <link rel="stylesheet" href="/assets/style.css?v={{ site.version }}">
  </head>
  <body>
    <a href="#main">Skip to content</a>
    <header>
      <a href="/" class="logo">
        {% include "logo.svg" %}
      </a>
      {% include "menu.njk" %}
    </header>
    <main id="main">
      {% block content %}{% endblock content %}
    </main>
    <a href="#top">Back to the top</a>
    <script src="/assets/index.js?v={{ site.version }}"></script>
  </body>
</html>
