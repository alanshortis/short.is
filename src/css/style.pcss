html {
  scroll-behavior: smooth;
}

.sr {
  clip-path: inset(100%);
  clip: rect(0, 0, 0, 0);
  height: 1px;
  overflow: hidden;
  position: absolute;
  white-space: nowrap;
  width: 1px;
}

.active {
  color: red;
}
