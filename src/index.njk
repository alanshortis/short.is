---
layout: 'page'
---

<section>
  <h1>Alan Shortis</h1>
  <p>—a front end developer based in Nottingham.</p>

  <div>
    <h2>Latest writing</h2>
    <ul>
      {% set latest_writing = collections.writing | reverse %}
      {% for post in latest_writing.slice(0, 1) %}
        <li>
          <article>
            <a href="{{ post.data.permalink }}">
              {%- set postDate = post.data.date %}
              {% include 'postdate.njk' %}
              <h2>{{ post.data.title }}</h2>
            </a>
          </article>
        </li>
      {%- endfor %}
    </ul>
    <a href="/writing">More writing</a>

    <h2>Daily</h2>
    <ul>
      {% set latest_daily = collections.daily | reverse %}
      {% for post in latest_daily.slice(0, 1) %}
        <li>
          <article>
            <a href="{{ post.data.permalink }}">
              {%- set postDate = post.data.date %}
              {% include 'postdate.njk' %}
              <h2>{{ post.data.title }}</h2>
            </a>
          </article>
        </li>
      {%- endfor %}
    </ul>
    <a href="/daily">More Dailies</a>
  </div>
</section>
