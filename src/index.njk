---
layout: 'page'
---

<section>
  <h1>Alan Shortis</h1>
  <p>—a front end developer based in Nottingham.</p>

  <div>
    <h2>Latest writing</h2>
    <ul>
      {%- for post in collections.latest_writing -%}
        <li>
          <article>
            <a href="{{ post.data.permalink }}">
              {%- set postDate = post.data.date %}
              {% include 'postdate.njk' %}
              <h2>{{ post.data.title }}</h2>
            </a>
          </article>
        </li>
      {%- endfor %}
    </ul>

    <h2>Daily</h2>
    <ul>
      {%- for post in collections.latest_daily -%}
        <li>
          <article>
            <a href="{{ post.data.permalink }}">
              {%- set postDate = post.data.date %}
              {% include 'postdate.njk' %}
              <h2>{{ post.data.title }}</h2>
            </a>
          </article>
        </li>
      {%- endfor %}
    </ul>
  </div>
</section>
